<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.staitech.fr.mapper.SysOrganizationMapper">

    <resultMap id="BaseResultMap" type="cn.staitech.fr.domain.SysOrganization">
        <id property="organizationId" column="organization_id"/>
        <result property="organizationName" column="organization_name"/>
        <result property="contactName" column="contact_name"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="organizationNumber" column="organization_number"/>
    </resultMap>
    <resultMap type="cn.staitech.fr.domain.OrganizationIdName" id="organizationMap">
        <result property="organizationId" column="organization_id"/>
        <result property="organizationName" column="organization_name"/>
    </resultMap>
    <sql id="Base_Column_List">
        organization_id
        ,organization_name,contact_name,phone_number,status,del_flag,
        create_by,create_time,update_by,update_time,organization_number
    </sql>

    <!--查询组织表IdNameList-->
    <select id="selectIdNameList" resultMap="organizationMap">
        select distinct organization_id, organization_name
        from sys_organization
        order by organization_id asc;
    </select>

    <select id="getOrganizationName" resultType="java.lang.String">
        select organization_name
        from sys_organization
        where organization_id = #{organizationId}
    </select>
</mapper>
